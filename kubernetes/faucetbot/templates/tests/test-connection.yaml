apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "faucetbot.fullname" . }}-test-connection"
  labels:
{{ include "faucetbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "faucetbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
