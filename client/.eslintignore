.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example

# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
yarn.lock

# host-api tests use @parity/host-api-test-sdk/playwright which needs
# moduleResolution:"bundler"; excluded from tsconfig so ESLint can't parse it
tests/host-api.test.ts
